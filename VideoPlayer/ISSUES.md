# ISSUES

1. Only one voice command seems to be executed. After that voice commands are ignored. This was probably caused by an attempt to fix the multiple execution of commands (an earlier issue).

2. The last file played is not re-opened on re-launch. This was a feature which was (attempted to be)
